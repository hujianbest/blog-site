# TASK-UI-005 Implementation Handoff

## Metadata

- Task ID: `TASK-UI-005`
- Task Title: Refactor Article Detail Reading Surface
- Date: 2026-05-13
- Current Stage: `hf-test-driven-dev`
- Workspace Isolation: in-place

## Scope Implemented

- Refactored `ArticleDetail.vue` shell background and article surface to Editorial Studio tokens.
- Added `data-ui="article-detail"` anchor.
- Added shared loading/error state anchors.
- Changed missing article state to a tokenized panel with "返回文章列表".
- Applied `ui-reading`, `text-[18px]`, and `leading-[1.8]` to article content.
- Tokenized masthead, tags, share button, previous/next navigation.
- Guarded adjacent article lookup against non-array response data.
- Normalized escaped `\n` sequences from API content before markdown rendering.
- Tokenized markdown code block wrapper generated by markdown-it.
- Tokenized `.markdown-content` code/pre/blockquote CSS to avoid legacy raw colors overriding generated classes.
- Strengthened browser smoke to fail on literal escaped newline text in rendered content.
- Demoted markdown `h1` to `h2` inside rendered article content to preserve one page-level `h1`.
- Added route param watcher so `/articles/:id` changes reload article content, adjacent links, and SEO.
- Tokenized markdown link/table CSS.

## Files Touched

- `frontend/src/views/ArticleDetail.vue`
- `frontend/src/views/__tests__/ArticleDetail.test.ts`
- `features/002-ui-design-refactor/evidence/task-ui-005-browser-smoke.cjs`
- `features/002-ui-design-refactor/evidence/task-ui-005-browser-smoke.json`
- `features/002-ui-design-refactor/evidence/task-ui-005-article-detail-desktop.png`
- `features/002-ui-design-refactor/evidence/task-ui-005-article-detail-mobile.png`

## RED Evidence

```text
npm test -- --run src/views/__tests__/ArticleDetail.test.ts
```

- Exit Code: 1
- Expected failures:
  - Loading state lacked `data-ui-state="loading"`.
  - Error state lacked `data-ui-state="error"` and "返回文章列表".
  - Article detail lacked `data-ui="article-detail"`, tokenized surface, and `ui-reading` rhythm.
- Test-review RED follow-up:
  - Escaped newline markdown test failed before content normalization.
  - Tokenized code block test failed before markdown-it emitted tokenized `pre` classes.
  - Markdown CSS source assertion would fail while `.markdown-content pre` still used `#1f2937`.

## GREEN Evidence

```text
npm test -- --run src/views/__tests__/ArticleDetail.test.ts src/__tests__/design-tokens.test.ts
```

- Exit Code: 0
- Result: 2 files passed, 12 tests passed
- Follow-up result: ArticleDetail/design-token tests passed after markdown normalization and code block tokenization.
- Final result: ArticleDetail/design-token tests passed after tokenizing markdown CSS.

```text
npx vite build
```

- Exit Code: 0
- Result: build succeeded
- Note: existing large chunk warning remains

```text
node features/002-ui-design-refactor/evidence/task-ui-005-browser-smoke.cjs
```

- Exit Code: 0
- Result: `/articles/1` desktop/mobile smoke passed, `issues: []`
- Smoke now includes `noLiteralEscapedNewlines: true`.
- Smoke now reports one page-level `h1`.

## Evidence Tier Coverage

| Tier | Required? | Evidence Provided | Status | Notes |
|---|---|---|---|---|
| mocked-unit | optional | Vitest tests | covered | supporting evidence |
| component-integration | required | ArticleDetail tests | covered | reading surface and state anchors |
| api-contract | required | smoke records article detail and article list requests status 200 | covered | no API behavior changed |
| browser-runtime | required | smoke JSON + screenshots | covered | article detail desktop/mobile |
| full-stack-smoke | N/A | frontend+backend used during smoke | N/A | no full-stack claim |

## UI Conformance Notes

- Article body uses `ui-reading`, `text-[18px]`, and `leading-[1.8]`.
- Article surface uses `color.bg.surface` and `color.border.default`.
- Tags use amber wash tokens.
- Error and loading states use explicit state anchors.
- Escaped API newline sequences are normalized before markdown rendering.
- Markdown code blocks receive tokenized dark surface classes.
- Markdown code/pre/blockquote CSS now uses design tokens instead of raw legacy colors.
- Markdown links and tables now use design tokens instead of raw blue/gray values.
- Route id changes reload article content.
- Full markdown styling still contains some legacy CSS values in `.markdown-content`; this task focuses the primary reading surface contract, while deeper markdown theme polish can be handled as follow-up if needed.

## Refactor Note

- Hat Discipline: visual reading surface changed within TASK-UI-005 scope.
- In-task Cleanups:
  - Replace Magic Literal with Named Token: replaced detail surface and masthead gray/blue styles with design tokens.
  - Introduce Contract Anchor: added `data-ui="article-detail"` and state anchors.
  - Guard Clause: guarded adjacent articles against non-array response data.
  - Normalize Input: converted escaped newline sequences before markdown rendering.
  - Replace Magic Literal with Named Token: applied token classes to generated markdown code blocks.
  - Preserve Heading Hierarchy: demoted markdown h1 output to h2 inside article content.
  - Add Reactive Route Handling: reload article content when `route.params.id` changes.
- Architectural Conformance: no route/API/store/module boundary changes.
- Documented Debt: deeper markdown blockquote/table typography polish remains possible follow-up.
- Escalation Triggers: none.
- Fitness Function Evidence: RED tests, GREEN tests, Vite build, browser smoke.

## Next Action

- `hf-test-review`
